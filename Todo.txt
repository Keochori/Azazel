// Models
- Normal Maps
- PBR materials
- Animations
- Transparency
- instance rendering (if they have same mesh)
- Quaternion rotation (Done for camera)

// Engine
- PhysX
- Deferred Rendering 
- Entity Component System
- ImGui
- Skybox
- Gizmo
- Render States
- add debug draw line
- add runtime window resizing changes
- Put bone data into structured buffer so MAX_BONES_PER_VERTEX doesnt crash program when changed

// Other
- Add debug text like framerate and stuff
- Disable engine inputs when ImGui is taking inputs
- png and other to .dds conversion
- in premake try to copy entire asset folder instead of individual folders to bin
- Fix jpg textures being darker
- Check how submeshes with different materials are supposed to be loaded in

- How does binding multiple buffers work? (inputassembler with VertexBuffers)
- Check if its worth separating World View Projection into multiple buffers
- Why is fps halved when alt+enter mode(windowed fullscreen)

// Repeated checks
- = default on all constructors
- check const& returns and arguments